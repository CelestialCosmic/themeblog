# 多模态数据
模态：听觉、嗅觉、味觉、触觉、视觉
## 早期融合、后期融合（前融合与后融合）
自动驾驶：图像+雷达
此时图像是一个模态，雷达是一个模态
雷达通过三角定位确定位置（天顶角+方位角+目标）
处理至 64x64 点阵 64x64 点云
1. 后期融合->先单个解析，后融合
2. 早期融合->先融合，再解析

# 中期融合
# 跨模态映射
## LLM架构
文本输入->文本嵌入（clip 转化为向量）->注意力->线性（将输出的权重做成线性的，再加上自回归）->输出
LLM本质也是分类模型

输入的图会切成一块一块的，然后做成向量序列（相当于token），再进行早期融合


## 图片
图片输入后，卷积展平，线性分类头
## 视频
视觉语言+计算机视觉+
vlm+cv+llm+rag
向量数据库+图形数据库
# VSS


