## HMAC
全称 **Hash-based Message Authentication Code** 
无论输入的消息有多长，HMAC 的输出长度都取决于所使用的哈希函数。
- 使用 SHA-256 → 输出是 256 位（32 字节）
- 使用 SHA-1 → 输出是 160 位（20 字节）

虽然输出和 SHA-256 长度相同，但比 SHA-256 更安全，因为它有一个密钥，如果是硬编码能找到